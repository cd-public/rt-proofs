(* For simplicity, we reuse the scheduler from the basic model.
   Although it does not exploit parallelism, it is a restricted case
   of a parallel scheduler. *)
Require Export rt.implementation.global.basic.schedule.